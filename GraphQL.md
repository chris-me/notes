# Code samples

## Queries

```graphql
query {
  users(where: {
    name_contains:"chris"
  }) {
    id
    name
    email
  }
}
```

## Mutations

```graphql
query {
  users(where: {
    name_contains:"chris"
  }) {
    id
    name
    email
  }
}
```
